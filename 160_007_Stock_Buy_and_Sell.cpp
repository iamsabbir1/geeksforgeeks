#include <bits/stdc++.h>
using namespace std;
int maximumProfit(vector<int> &prices)
{
    // code here
}
int main()
{
    int n;
    cin >> n;
    vector<int> prices(n);

    for (int i = 0; i < n; i++)
        cin >> prices[i];
    cout << maximumProfit(prices);
}